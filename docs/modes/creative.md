# Creative Mode

Routing bias: **generate** (direct response; no tool/retrieve routing).

```
User Query
   |
   v
Route Query (mode bias = generate)
   |
   v
Generate Response (high-creativity system prompt)
   |
   v
END
```
