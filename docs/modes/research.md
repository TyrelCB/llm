# Research Mode

Routing bias: **web** (goes directly to web search + synthesis).

```
User Query
   |
   v
Route Query (mode bias = web)
   |
   v
Web Search
   |-- Search (DuckDuckGo / SearXNG)
   |-- Fetch top pages (optional)
   |-- Synthesize with citations
   |
   v
Response (answer + sources + next steps)
```
